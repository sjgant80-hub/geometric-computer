[
  {
    "mutation": "< → <=",
    "snippet": "for (let y = 1; y < p; y++) if ((aa * y) % p === 1) return y;",
    "reason": "fold.mjs — EQUIVALENT: the modular inverse of a coprime residue exists in [1,p-1], so the loop returns before y reaches p; the extra iteration y=p never matches ((aa*p)%p=0≠1). No behaviour change."
  },
  {
    "mutation": "> → >=",
    "snippet": "if (a > R) R = a;",
    "reason": "eth.mjs — EQUIVALENT: > and >= differ only when a===R, and assigning R=a when a===R is a no-op. Running maximum unchanged."
  },
  {
    "mutation": "> → >=",
    "snippet": "if (!Number.isFinite(x) || Math.abs(x) > 1e6) {",
    "reason": "eth.mjs — EQUIVALENT: the escape threshold at exactly 1e6 is dynamically unreachable — near escape the quadratic map's magnitude roughly squares each step, jumping past 1e6, never landing on it. > vs >= at the exact boundary is never exercised."
  },
  {
    "mutation": "> → >=",
    "snippet": "const mags = sig.map((v) => Math.abs(num(v))).filter((v) => Number.isFinite(v) && v > 0)",
    "reason": "eth.mjs — EQUIVALENT: including zero-magnitude components (v>=0) never changes the result — a 0 sorts to the bottom, so it is never the dominant component and, as mags[1], trips the mags[1]<1e-12 guard to the same return 0."
  },
  {
    "mutation": "> → >=",
    "snippet": "return Number.isFinite(v) && v > 0 && SEAM % v === 0 && v !== 1;",
    "reason": "eth.mjs — EQUIVALENT: > and >= differ only at v===0, where both yield false (v>0 short-circuits false; v>=0 then hits SEAM%0=NaN !== 0)."
  },
  {
    "mutation": "< → <=",
    "snippet": "if (x < 1e-9) return 20;                 // integer winding = fully commensurate = maximal",
    "reason": "eth.mjs — EQUIVALENT: this guards the integer-winding case where the fractional part x is 0; at x=0 both < and <= return 20. A winding with fractional part exactly 1e-9 is not constructible deterministically (measure-zero boundary)."
  },
  {
    "mutation": "< → <=",
    "snippet": "for (let i = 0; i < 8; i++) {",
    "reason": "eth.mjs — EQUIVALENT (reviewed tuning cap): the continued-fraction expansion depth (8 terms) is a tuning parameter; the resonance discriminator is robust to ±1 term (the large coefficient that separates low-order rationals appears within the first few terms). The measured discrimination (noble 86% vs rational 0%) is unchanged at 8 or 9."
  },
  {
    "mutation": "< → <=",
    "snippet": "if (x < 1e-9) { R = 20; break; }       // CF terminated = exact rational = max resonance",
    "reason": "eth.mjs — EQUIVALENT: guards the CF-terminated (exact-rational) case where x reaches 0; at x=0 both < and <= fire R=20 and break. The exact-1e-9 boundary is unreachable (measure-zero)."
  },
  {
    "mutation": "< → <=",
    "snippet": "if (mags.length < 2 || mags[1] < 1e-12) return 0;",
    "reason": "eth.mjs — EQUIVALENT (the mags[1]<1e-12 occurrence): a second component of magnitude exactly 1e-12 is not constructible deterministically (measure-zero boundary). The mags.length<2 occurrence on this line IS killed (windingOf([8,5]) test)."
  },
  {
    "mutation": "+ 1 → - 1",
    "snippet": "for (let j = i; j < T; j++) s.push((j % 2 ? 1 : -1) * 1e6 * (j + 1));",
    "reason": "eth.mjs — EQUIVALENT: the escape-fill only needs to make the classifier read ESCAPED; both (j+1) and (j-1) produce an alternating envelope whose peaks expand to the end, so the classification (and every downstream verdict) is identical."
  }
]
